import * as <%- className -%>Util from './<%- name -%>.utils';

describe('<%- className -%>Util', () => {
    describe('Util method', () => {
        it('should...', () => {
            expect(false).toBeTruthy();
        });
    });
});
