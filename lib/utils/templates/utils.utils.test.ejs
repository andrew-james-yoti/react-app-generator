import * as <%- className -%>Util from './<%- name -%>.utils.js';

describe('<%- className -%>', () => {
    describe('Util method', () => {
        it('should...', () => {
            expect(false).toBeTruthy();
        });
    });
});
