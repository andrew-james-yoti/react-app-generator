import React from 'react';
import {
    render,
    cleanup,
} from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import <%- className -%> from './<%- name -%>.jsx';

const getElement = () => (
    render(
        <<%- className -%> />,
    )
);

describe('<%- className -%>', () => {
    describe('Component Elements', () => {
        it('should ...', () => {
            expect(false).toBeTruthy();
        });
    });
});